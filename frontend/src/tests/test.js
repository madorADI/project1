describe("formatDate", () => {
  test("increments the current number by 1", () => {
    expect(1).toBe(1);  
  });
  test("increments the current number by 1", () => {
    expect(1).toBe(1);
  });
  test("increments the current number by 1", () => {
    expect(1).toBe(1);
  });
  test("increments the current number by 1", () => {
    expect(1).toBe(1);
  });
});

describe("formatDate", () => {
  test("increments the current number by 1", () => {
    expect(1).toBe(1);
  });
  test("increments the current number by 1", () => {
    expect(1).toBe(1);
  });
  test("increments the current number by 1", () => {
    expect(1).toBe(1);
  });
  test("increments the current number by 1", () => {
    expect(1).toBe(1);
  });
});
